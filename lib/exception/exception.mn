load system.terminal

class Exception {
	def show(self) {
		self.printError('')
	}

	#def printError(self, error) {
		print (System.stderr) {
			System.IO.bold()
			System.IO.red()
			(typeof self) + ": " + error
			System.IO.reset()
			'\n'
		}
	}
}
