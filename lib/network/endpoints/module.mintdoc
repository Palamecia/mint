This module provides types and utilities for representing and working with network endpoints. It defines common
abstractions for endpoint addresses and connection parameters, and provides functionality for creating, validating,
and resolving endpoint information used in network communication.

```mn
socket.connect(Network.IpEndpoint('1.2.3.4', 42))
```
