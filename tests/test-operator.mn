#!/bin/mint

load test.case

class OperatorTest : TestCase {

	def test_add(self) {

		self.expectEqual(4, 2 + 2)
		self.expectEqual(4, 2 + '2')

		self.expectEqual('22', '2' + '2')
		self.expectEqual('22', '2' + 2)
	}
}

enable_test_case(OperatorTest)
